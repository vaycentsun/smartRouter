# V3 配置别名清理实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清理 v3_schema.py 和 v3_loader.py 别名文件，统一使用 schema.py/loader.py

**Architecture:** v3 别名模块仅做重定向，实际代码在 schema.py/loader.py 中。清理方案：将所有引用迁移到直接导入，然后删除别名文件。

**Tech Stack:** Python, Pytest

---

## 当前引用分析

项目中有 **23 处** 引用了 v3 相关模块：

| 文件 | 引用 |
|------|------|
| `test_v3_integration.py` | v3_loader, v3_schema, v3_selector |
| `test_plugin_v3_adapter.py` | v3_schema |
| `test_v3_schema.py` | v3_schema |
| `test_loader.py` | v3_loader |
| `test_v3_selector.py` | v3_selector（selector 模块，非 config） |
| `plugin_v3_adapter.py` | v3_loader, v3_schema |
| `gateway/test_server.py` | v3_schema |

---

## v3 别名文件内容

### v3_schema.py（33 行）
```python
from .schema import (
    ProviderConfig,
    ModelCapabilities,
    ModelConfig,
    TaskConfig,
    DifficultyConfig,
    StrategyConfig,
    FallbackConfig,
    RoutingConfig,
    Config,
)
ConfigV3 = Config
```

### v3_loader.py（21 行）
```python
from .loader import ConfigLoader, ConfigError, load_config
ConfigV3Loader = ConfigLoader
ConfigV3Error = ConfigError
load_v3_config = load_config
```

---

## Task 1: 迁移 v3_schema 引用

**Files:**
- Modify: `src/smart_router/config/__init__.py`
- Modify: `src/smart_router/router/plugin_v3_adapter.py`
- Modify: `src/smart_router/gateway/tests/test_server.py`
- Modify: `src/smart_router/router/tests/test_plugin_v3_adapter.py`
- Modify: `src/smart_router/tests/integration/test_v3_integration.py`
- Modify: `src/smart_router/config/tests/test_v3_schema.py`

- [ ] **Step 1: 在 config/__init__.py 统一导出**

更新 `src/smart_router/config/__init__.py`，确保导出所有需要的类：

```python
from .schema import (
    ProviderConfig,
    ModelCapabilities,
    ModelConfig,
    TaskConfig,
    DifficultyConfig,
    StrategyConfig,
    FallbackConfig,
    RoutingConfig,
    Config,
)
from .loader import ConfigLoader, ConfigError, load_config

__all__ = [
    "ProviderConfig",
    "ModelCapabilities",
    "ModelConfig",
    "TaskConfig",
    "DifficultyConfig",
    "StrategyConfig",
    "FallbackConfig",
    "RoutingConfig",
    "Config",
    "ConfigLoader",
    "ConfigError",
    "load_config",
]
```

- [ ] **Step 2: 更新 test_v3_integration.py 引用**

```python
# 旧（删除）
from smart_router.config.v3_schema import ConfigV3
from smart_router.config.v3_loader import ConfigV3Loader

# 新
from smart_router.config import Config, ConfigLoader
```

- [ ] **Step 3: 更新 test_plugin_v3_adapter.py 引用**

```python
# 旧
from smart_router.config.v3_schema import ConfigV3

# 新
from smart_router.config import Config
```

- [ ] **Step 4: 更新 test_v3_schema.py 引用**

```python
# 旧
from smart_router.config.v3_schema import ...

# 新
from smart_router.config import ...
```

- [ ] **Step 5: 运行测试验证**

```bash
pytest src/smart_router/config/tests/test_v3_schema.py -v
```

- [ ] **Step 6: 提交**

```bash
git add src/smart_router/config/__init__.py src/smart_router/tests/integration/test_v3_integration.py
git commit -m "refactor(config): 迁移 v3_schema 引用到直接导入"
```

---

## Task 2: 迁移 v3_loader 引用

**Files:**
- Modify: `src/smart_router/config/tests/test_loader.py`
- Modify: `src/smart_router/router/plugin_v3_adapter.py`

- [ ] **Step 1: 更新 test_loader.py 引用**

```python
# 旧
from smart_router.config.v3_loader import ConfigV3Loader, ConfigV3Error, load_v3_config

# 新
from smart_router.config import ConfigLoader, ConfigError, load_config
```

- [ ] **Step 2: 更新 plugin_v3_adapter.py 引用**

```python
# 旧
from ..config.v3_loader import ConfigV3Loader
from ..config.v3_schema import ConfigV3

# 新
from ..config import ConfigLoader
from ..config import Config
```

- [ ] **Step 3: 运行测试验证**

```bash
pytest src/smart_router/config/tests/test_loader.py -v
```

- [ ] **Step 4: 提交**

```bash
git add src/smart_router/config/tests/test_loader.py src/smart_router/router/plugin_v3_adapter.py
git commit -m "refactor(config): 迁移 v3_loader 引用到直接导入"
```

---

## Task 3: 删除 v3 别名文件

**Files:**
- Delete: `src/smart_router/config/v3_schema.py`
- Delete: `src/smart_router/config/v3_loader.py`

- [ ] **Step 1: 确认无其他引用**

```bash
grep -r "v3_schema\|v3_loader" src/smart_router/ --include="*.py"
```

确保无结果。

- [ ] **Step 2: 删除别名文件**

```bash
rm src/smart_router/config/v3_schema.py
rm src/smart_router/config/v3_loader.py
```

- [ ] **Step 3: 运行完整测试验证**

```bash
pytest -v --tb=short 2>&1 | tail -30
```

- [ ] **Step 4: 提交**

```bash
git add src/smart_router/config/
git commit -m "refactor(config): 删除 v3 别名文件，统一使用 schema.py/loader.py"
```

---

## 验证检查点

```bash
# 1. 无 v3_ 引用
grep -r "from.*v3_schema\|from.*v3_loader" src/ || echo "✓ 无引用"

# 2. 测试通过
pytest -v --tb=short

# 3. 导入仍然可用
python -c "from smart_router.config import Config, ConfigLoader"
```

---

**Plan complete and saved to `docs/superpowers/plans/2026-04-27-v3-cleanup.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**